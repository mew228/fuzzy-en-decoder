# Local dev — Vite proxies /api → localhost:8000, no env var needed.
# Production — set VITE_API_URL in Vercel dashboard to your Railway backend URL.
# VITE_API_URL=https://your-backend.up.railway.app
